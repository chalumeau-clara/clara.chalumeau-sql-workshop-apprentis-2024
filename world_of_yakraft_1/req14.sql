SELECT title FROM character_quests INNER JOIN character ON character_quests.character_id=character.id INNER JOIN quest ON character_quests.quest_id=quest.id WHERE name='Kuro' AND complete=1 ORDER BY title;
